# Architecture

## Overview

This monorepo contains a React/Vite web application, a Hono/Cloudflare Workers API, and shared packages.

## Boundaries

- `factory-generated` — files owned and synced by Project Factory.
- `features` — product capabilities added by modules.
- `extensions` — safe space for manual user extensions.
