#!/usr/bin/env node
/**
 * create-projectforge — executable stub (Phase 1)
 * Full implementation will be wired to the built dist in Phase 2/3.
 */

const [projectName] = process.argv.slice(2);
console.log(`create-projectforge: would create "${projectName || 'my-project'}" (not implemented in Phase 1)`);
