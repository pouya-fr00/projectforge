# No Central Service Policy

Project Factory V1 must remain useful if the maintainer disappears and the npm/Git package has already been downloaded.

No required:

- login;
- API key;
- remote module resolution endpoint;
- license server;
- telemetry collector;
- cloud database;
- paid API.

Static docs and package distribution may use public hosting, but generated projects do not depend on them at runtime.
